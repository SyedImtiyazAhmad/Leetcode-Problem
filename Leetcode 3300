class Solution {
    public int minElement(int[] nums) {
        int a=nums[0];
      for(int i=0;i<nums.length;i++){
        int b=0;
        while(nums[i]>0){
            b+=nums[i]%10;
            nums[i]/=10;
        }
        a=Math.min(a,b);
      }
      return a; 
    }
}
